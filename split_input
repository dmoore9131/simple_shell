#include "shell.h"

int main(void)
{
    char *input;
    char **args;

    while (1)
    {
        input = get_input();
        args = split_input(input);
        execute(args);
        free(input);
        free_args(args); // Implement this function to free the arguments array
    }

    return (0);
}

