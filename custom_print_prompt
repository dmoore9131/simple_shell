#include "custom_shell.h"

/**
 * custom_print_prompt - Display a custom shell prompt on the standard output.
 *
 * This function prints a custom shell prompt to the standard output stream.
 * The prompt is typically displayed to indicate that the shell is ready to accept input.
 */
void custom_print_prompt(void)
{
    _puts(CUSTOM_PROMPT); // Print the custom shell prompt
    fflush(stdout); // Flush the output stream
}
#include <stdio> 
